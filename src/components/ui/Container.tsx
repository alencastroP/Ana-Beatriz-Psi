import styled from 'styled-components'

/** Largura maxima centralizada usada por todas as secoes. */
export const Container = styled.div`
  width: 100%;
  max-width: 1140px;
  margin: 0 auto;
  padding: 0 24px;
`
